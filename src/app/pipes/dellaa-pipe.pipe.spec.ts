import { DellaaPipePipe } from './dellaa-pipe.pipe';

describe('DellaaPipePipe', () => {
  it('create an instance', () => {
    const pipe = new DellaaPipePipe();
    expect(pipe).toBeTruthy();
  });
});
